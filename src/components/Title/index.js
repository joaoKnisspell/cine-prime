import styled from "styled-components";

export const Title = styled.h1`
    font-size: 20px;
    color: var(--branco);
    font-weight: 600;
    @media screen and (min-width: 1024px) {
        font-size: 26px;
    }   
`